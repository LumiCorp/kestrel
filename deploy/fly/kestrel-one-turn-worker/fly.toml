app = "kestrel-one-turn-worker"
primary_region = "iad"
kill_signal = "SIGTERM"
kill_timeout = 35

[build]
  dockerfile = "Dockerfile"

[processes]
  worker = "pnpm --filter @kestrel/kestrel-one worker:turns"

[env]
  KESTREL_WORKER_HEALTH_PORT = "8081"

[checks.worker]
  type = "http"
  port = 8081
  method = "GET"
  path = "/healthz"
  interval = "15s"
  timeout = "5s"
  grace_period = "30s"

[[vm]]
  processes = ["worker"]
  cpu_kind = "shared"
  cpus = 2
  memory = "2gb"
