---
title: kcron
summary: Run and schedule repeatable Kestrel tasks for registered workspaces.
section: cli
audience: everyone
status: active
sourceKind: curated
updatedAt: 2026-06-16
---

<WorkspaceCopilotDemo step="CLI mechanics">
Use `kcron` to run the Workspace Copilot review on a schedule and write the result to `./reports`.
</WorkspaceCopilotDemo>

`kcron` is a Beta local automation tool. Run the task once manually before installing a schedule, and confirm installation and failure behavior on the target machine before depending on it.

Recurring execution is available only where the workspace and packaged scheduler support it. The workspace catalog may show `automationEnabled` before scheduled execution is enabled for that installation.

## Core commands

```bash
kcron start
kcron stop
kcron status
kcron run-once
kcron install
kcron uninstall
```

## Workspace Copilot workflow

```bash
kestrel workspace status
kestrel workspace list
kcron install
kcron status
kcron run-once
```

`run-once` is the safest first validation step once scheduler execution is available because it proves the configured run path before the schedule matters.

## What each command is for

| Command | Use it for |
| --- | --- |
| `install` | install or enable the daemon |
| `start` | start the scheduler manually |
| `status` | verify the daemon is healthy |
| `run-once` | test the exact workspace cron flow now |
| `stop` | stop scheduling without uninstalling |
| `uninstall` | remove the scheduler integration entirely |

## Incident checklist: nightly report missing

- [ ] `kestrel workspace status` shows the expected catalog entry
- [ ] `kestrel workspace list` shows the expected root path
- [ ] the catalog entry has the intended `automationEnabled` value
- [ ] the central scheduler implementation is available for recurring runs
- [ ] run output and replay evidence are inspectable after execution

## Continue with scheduled work

- [Workspace workflows](/cli/workspace-workflows)
- [Automating common tasks](/build/automating-common-tasks)
- [Production operating model](/operate/deployment)
