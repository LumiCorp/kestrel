# Bind in-memory prestarted runs to trusted tenant authority

The in-memory conversation-turn claim path must persist the store-owned tenant, validate mismatches, and reconcile trusted legacy undefined ownership consistently with PostgreSQL. Effects must inherit that owner and support the same atomic cancel/result contract.
